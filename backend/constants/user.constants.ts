export enum UserRole {
  STUDENT = "student",
  STAFF_MEMBER = "staffMember",
  VENDOR = "vendor",
  ADMINISTRATION = "administration",
  UNKNOWN = "unknown"
}

export enum UserStatus {
    ACTIVE = "active",
    BLOCKED = "blocked"
}